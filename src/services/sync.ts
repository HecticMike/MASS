import type { AppState, Entry } from '../types/app'

export const syncToGoogleSheets = async (_entries: Entry[]): Promise<void> => {
  return Promise.resolve()
}

export const syncToDriveJson = async (_state: AppState): Promise<void> => {
  return Promise.resolve()
}

